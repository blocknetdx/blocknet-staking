// const config = require('../config');
// const io = require("socket.io-client");

// Websocket for cors
let socket = null //io.connect(config.server.url, { transports : ['websocket'] });

module.exports = socket;